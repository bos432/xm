# 验收测试报告

## 1. 测试结论

测试日期：2026-07-02  
测试站点：https://nxm.zlck888.com  
测试方式：浏览器自动化回归 + 服务器健康检查  
测试结果：通过

```text
9 passed
```

## 2. 服务器健康检查结果

已在宝塔终端执行：

```bash
/www/server/php/83/bin/php artisan health:login-check \
  --username=health_check_user \
  --password='HealthCheck-2026'
```

结果：

```text
Health login passed for [health_check_user].
```

接口检查：

```bash
curl -s https://nxm.zlck888.com/api/auth/captcha
curl -s https://nxm.zlck888.com/api/public/homepage | head -c 500
curl -I https://nxm.zlck888.com/
curl -I https://nxm.zlck888.com/lifecycle
```

结果：

- 验证码接口正常。
- 首页内容接口正常。
- 首页返回 `HTTP/2 200`。
- 全周期路由返回 `HTTP/2 200`。

## 3. 浏览器回归覆盖

| 序号 | 用例 | 结果 |
| --- | --- | --- |
| 1 | 公开首页可访问，登录入口正常，无重复旧“单位注册”入口 | 通过 |
| 2 | 单位用户登录，查看项目、全周期、验收历史 | 通过 |
| 3 | 区县角色登录，查看项目、全周期、验收历史 | 通过 |
| 4 | 部门角色登录，查看项目、全周期、验收历史 | 通过 |
| 5 | 专家角色登录，查看项目、全周期、验收历史 | 通过 |
| 6 | 管理员登录，查看项目、全周期、验收历史 | 通过 |
| 7 | 超级管理员登录，查看项目、全周期、验收历史 | 通过 |
| 8 | 单位、区县、部门无专家认证权限；专家可访问专家认证接口 | 通过 |
| 9 | 超级管理员检查首页素材状态和安全中心 | 通过 |

本次自动化报告路径：

```text
modernization/e2e/reports/runs/20260702-final-delivery/html/index.html
modernization/e2e/reports/runs/20260702-final-delivery/results.json
```

本次截图路径：

```text
modernization/e2e/reports/runs/20260702-final-delivery/artifacts
```

## 4. 测试账号

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 单位用户 | `e2e_20260630_103223_unit` | `Test@2026pass` |
| 区县审核 | `e2e_20260630_103223_county` | `Test@2026pass` |
| 部门审核 | `e2e_20260630_103223_department` | `Test@2026pass` |
| 专家评审 | `e2e_20260630_103223_expert` | `Test@2026pass` |
| 管理员 | `e2e_20260630_103223_admin` | `Test@2026pass` |
| 超级管理员 | `admin` | `ChangeMe-2026` |

## 5. 测试数据

| 项目 | 内容 |
| --- | --- |
| 测试数据前缀 | `E2E-20260630-103223` |
| 测试项目 ID | `5` |
| 数据处理方式 | 保留，不物理删除 |
| 后台治理 | 可按“测试数据”筛选，超管可归档测试批次 |

## 5.1 权限回归结果

| 角色 | 专家认证菜单/页签 | `/api/lifecycle/expert-certifications` |
| --- | --- | --- |
| 单位用户 | 不可见 | `403` |
| 区县审核 | 不可见 | `403` |
| 部门审核 | 不可见 | `403` |
| 专家评审 | 可见 | `200` |

## 6. 已知非阻塞提示

| 提示 | 说明 |
| --- | --- |
| `PHP Warning: Module "mbstring" is already loaded` | PHP 扩展重复加载提示，不影响系统运行 |
| Composer 阿里云镜像 404 后继续下载 | Composer 自动换源下载成功，不影响部署 |
| Vite `INVALID_ANNOTATION` | 第三方库构建提示，不影响前端产物 |
| 大 chunk warning | 前端性能治理建议项，不阻塞交付 |

## 7. 验收结论

系统已完成本轮整改开发、部署和主流程回归。权限回归、六角色登录、项目筛选、全周期、验收历史、首页素材、安全中心均通过。建议甲方按真实业务账号再进行一次人工验收，重点检查正式首页素材、邮件 SMTP、真实申报批次和正式权限分配。
